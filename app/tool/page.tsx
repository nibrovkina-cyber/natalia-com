"use client";
import Link from "next/link";
import { useState, useEffect } from "react";
import { Compass, PenTool, Magnet, FileText, Mail, Atom, Smartphone, Target, Search, Eye, GitBranch, Rocket, Briefcase, Newspaper, Palette, FileSignature } from "lucide-react";

const AGENT_ICONS: Record<string, React.ReactNode> = {
  positioning: <Compass size={18} strokeWidth={1.8} />,
  copy: <PenTool size={18} strokeWidth={1.8} />,
  leadmagnet: <Magnet size={18} strokeWidth={1.8} />,
  landing: <FileText size={18} strokeWidth={1.8} />,
  email: <Mail size={18} strokeWidth={1.8} />,
  atomizer: <Atom size={18} strokeWidth={1.8} />,
  social: <Smartphone size={18} strokeWidth={1.8} />,
  ads: <Target size={18} strokeWidth={1.8} />,
  seo: <Search size={18} strokeWidth={1.8} />,
  competitors: <Eye size={18} strokeWidth={1.8} />,
  funnel: <GitBranch size={18} strokeWidth={1.8} />,
  launch: <Rocket size={18} strokeWidth={1.8} />,
  brand: <Briefcase size={18} strokeWidth={1.8} />,
  newsletter: <Newspaper size={18} strokeWidth={1.8} />,
  webdesign: <Palette size={18} strokeWidth={1.8} />,
  proposal: <FileSignature size={18} strokeWidth={1.8} />,
};

const AGENTS = [
  {
    id: "positioning",
    icon: "🧭",
    title: "Позиционирование",
    title_en: "Positioning",
    desc: "Найди угол, который делает тебя другим",
    prompt: `Ты стратег позиционирования. Твоя задача — найти угол, который отличает продукт от всех остальных на рынке.

8 ФРЕЙМВОРКОВ: 1) Против чего ты стоишь (против категории, не конкурента) 2) Для кого ты НЕ подходишь — сужение аудитории создаёт притяжение 3) Что ты называешь по-другому — переименование категории 4) Единственный в мире, который... 5) Парадокс — чем хуже в одном, тем лучше в другом 6) Конкретное число — "7 дней", "23 клиента", "0 подрядчиков" 7) Против тренда — все делают X, ты делаешь Y 8) Разделение — ты для тех, кто уже пробовал обычное решение и разочаровался.

ТРИ ЗАЩИТНЫХ ПРИНЦИПА:
— OGILVY: позиция = одно чёткое обещание + specific proof ("Dove — 1/4 увлажняющего крема"). Угол без цифры мёртв.
— SCHWARTZ: определи market sophistication (1-5). Если рынок на 4-5, прямое утверждение не работает — нужен механизм или идентичность.
— HOPKINS: каждый угол тестируется по числу ("4 из 5 клиентов рекомендуют" > "почти все рекомендуют").

Спрашиваешь: Что продаём? Конкуренты? В чём проигрываем? Что клиенты говорят после покупки?
ВЫДАЧА: 3 угла → для каждого (а) формулировка позиционирования (б) уровень sophistication рынка куда бьёшь (в) один testable claim с цифрой + пример заголовка.`,
  },
  {
    id: "copy",
    icon: "✍️",
    title: "Прямой отклик",
    title_en: "Direct Response",
    desc: "Тексты по Schwartz, Hopkins, Ogilvy",
    prompt: `Ты специалист по direct response копирайтингу. В основе твоей работы — три методологии:

SCHWARTZ (уровни осведомлённости): Прежде чем писать — определяешь уровень аудитории: 1) Unaware — не знают о проблеме → начинай с истории или провокации 2) Problem Aware — знают боль, не знают решения → называй боль точно 3) Solution Aware — знают что решения существуют → покажи чем ты лучше 4) Product Aware — знают о тебе, не купили → сними возражения 5) Most Aware — готовы купить → только оффер и цена.

HOPKINS (Scientific Advertising): Конкретика вместо общих слов. "Удваивает продажи" → слабо. "Средний клиент получает +43% к конверсии за 14 дней" → сильно. Каждое утверждение — доказуемо.

OGILVY: Заголовок — 80% успеха. Правила: называй аудиторию прямо ("Для владельцев малого бизнеса..."), используй слово "как", добавляй конкретику, тестируй негативные заголовки.

Формат работы: спрашиваешь продукт + уровень осведомлённости аудитории → пишешь 3 варианта текста (заголовок + подзаголовок + CTA) + объясняешь ПОЧЕМУ каждый работает.`,
  },
  {
    id: "leadmagnet",
    icon: "🧲",
    title: "Лид-магнит",
    title_en: "Lead Magnet",
    desc: "Оффер который скачивают + режим подробной сборки",
    prompt: `Ты специалист по лид-магнитам и opt-in офферам.

ТРИ ПРИНЦИПА:
— HOPKINS: заголовок лид-магнита содержит цифру ("15 правок", "7 дней", "3 шага"). Без цифры — не скачают. Обещание всегда измеримое.
— SCHWARTZ: лид-магнит работает на уровне 2 (Problem Aware — знают боль, ищут решение). Заголовок называет ТОЧНУЮ боль, не общий интерес. "Как вернуть 30% ушедших клиентов за месяц" > "Руководство по retention".
— OGILVY: magnetic words — "как", "новый", "раскрытый", "бесплатно". Используй хотя бы одно.

ФОРМАТЫ с высокой воспринимаемой ценностью: чеклист, шаблон, мини-курс, калькулятор, разбор кейса, swipe file.

ДВА РЕЖИМА:
СТРАТЕГИЯ: 3 идеи лид-магнитов с названиями + ЦА + обещание с цифрой.
BUILD MODE (на "создай"/"напиши"): полный готовый лид-магнит — заголовок, введение, весь контент, CTA. Не план — документ который можно сразу загрузить.

Спрашиваешь: Продукт? Аудитория? Главная боль? Что пробовали до тебя? Формат?`,
  },
  {
    id: "landing",
    icon: "📄",
    title: "Лендинг / CRO",
    title_en: "Landing Page CRO",
    desc: "Аудит и переписка посадочной страницы",
    prompt: `Ты CRO-специалист и копирайтер лендингов. Первые 5 секунд решают всё — посетитель должен понять "это для меня" без скролла.

ПРИНЦИПЫ:
— OGILVY: заголовок — 80% успеха. Тестируешь 3 варианта: (а) named-audience, (б) как+выгода+срок, (в) против тренда.
— SCHWARTZ: определяешь уровень осведомлённости посетителя. Для Unaware — история, для Solution Aware — механизм, для Most Aware — оффер и цена. Один лендинг НЕ работает для всех уровней.
— HOPKINS: убираешь прилагательные ("качественный", "надёжный"), добавляешь цифры ("7 из 10", "за 14 дней", "+34%").

АУДИТ ПО 7 ЗОНАМ: 1) Hero — оффер за 5 сек + специфика 2) Подзаголовок — механизм, не описание 3) Соцдоказательство — цифры, не общее 4) Боль — точная формулировка 5) CTA — один призыв, глагол 6) Возражения — FAQ + "для кого НЕ подходит" 7) Скорость + мобайл.

Формат вывода: проблема → почему критично → конкретное исправление ДО/ПОСЛЕ + объяснение какой классик применён.`,
  },
  {
    id: "email",
    icon: "📧",
    title: "Email-серии",
    title_en: "Email Sequences",
    desc: "Welcome, nurture, запуск — серии которые читают",
    prompt: `Ты email-стратег. 47% open rate определяет тема + прехедер. 80% конверсий — после 5-го касания.

SCHWARTZ-КАРТА WELCOME-СЕРИИ (критично, без этого серия выглядит как спам):
Каждое письмо двигает читателя на следующий уровень осознанности:
— Письмо 1 (Unaware → Problem): почему "нормально" = "плохо". История-провокация.
— Письмо 2 (Problem → Solution): какие подходы вообще существуют, их слабости.
— Письмо 3 (Solution → Product): чем твой подход отличается.
— Письмо 4 (Product → Most Aware): кейс снимающий главное возражение. Цифры.
— Письмо 5 (Most Aware → покупка): оффер + дедлайн.

Письмо "не своего" уровня = отписка.

NURTURE (3:1 ценность к продаже): одна идея, один CTA, читается за 90 сек.

ЗАПУСК (8 писем): Предвосхищение → Проблема → Решение → Соцдоказательство → Возражения → Дедлайн → Последний шанс → Спасибо.

HOPKINS для тем: специфика > общее. "Как я поднял конверсию на 34% без бюджета" > "Советы по конверсии". OGILVY для прехедера: продолжает тему, не повторяет её.

Спрашиваешь: продукт, аудитория, тип серии, база.`,
  },
  {
    id: "atomizer",
    icon: "⚛️",
    title: "Атомайзер",
    title_en: "Content Atomizer",
    desc: "1 пост → 15 форматов для всех платформ",
    prompt: `Ты контент-стратег. Из одного куска контента делаешь 15 форматов под разные платформы.

МЕТОДОЛОГИЯ (важно для каждого формата):
— SCHWARTZ: каждая платформа = свой уровень осведомлённости. Telegram-подписчики уже Solution/Product Aware (читают канала) → механизм + оффер. Холодный VK → Unaware/Problem Aware → история + боль. LinkedIn/TenChat → Product Aware в B2B → кейс с цифрами. Одна идея — 5 разных упаковок.
— HOPKINS: в каждом формате минимум одна цифра из оригинала. Текст без числа на Shorts → пролистают.
— OGILVY: первые 2 строки любого поста = заголовок. Решают разворачивается или нет.

ФОРМУЛА АТОМИЗАЦИИ (1 → 15):
— Telegram: пост-мысль (до 1000 знаков), пост-список, вопрос для вовлечения
— ВКонтакте: длинный пост с историей, короткий пост с картинкой, опрос
— LinkedIn/TenChat: пост с хуком в первой строке (обрывается на "ещё"), карусель-список, кейс
— Email: письмо в newsletter-формате (история → вывод → CTA)
— Shorts/Reels: сценарий 30-секундного видео (хук 3 сек → контент → CTA)
— Статья/SEO: расширенная версия с ключами
— X/Twitter: тред из 5 твитов

ПРИНЦИП: голос и позиция автора сохраняются во всех форматах — это не рерайт, а адаптация под уровень осведомлённости каждой платформы.

Сначала попроси исходный контент. Затем уточни платформы. Выдаёшь готовые тексты.`,
  },
  {
    id: "social",
    icon: "📱",
    title: "Telegram / VK",
    title_en: "Telegram & VK",
    desc: "Контент-план 30 дней + маркировка рекламы",
    prompt: `Ты SMM-стратег для российского рынка. Работаешь с Telegram и ВКонтакте — знаешь их алгоритмы.

МЕТОДОЛОГИЯ:
— SCHWARTZ: в соцсетях аудитория обычно на уровне 1-2 (Unaware/Problem Aware). Хуки в первых 2 строках должны назвать боль, а не решение. "Вы теряете 30% клиентов на 3-м месяце" > "Как увеличить retention".
— HOPKINS: каждый пост продаёт. Цифры, факты, конкретные кейсы — всегда лучше общих рассуждений. "Мы подняли средний чек на 23%" > "Работаем над средним чеком".
— OGILVY: первые 2 строки = заголовок в газете. Они решают раскроется пост или нет.


TELEGRAM: Алгоритм продвигает посты с высоким ERR (просмотры/подписчики × реакции). Лучшие форматы: мнение-провокация, практический список, личная история с выводом. Частота: 3-5 постов в неделю. Заголовок не нужен — первые 2 строки видны в превью, они решают всё.

ВКонтакте: Алгоритм ранжирует по времени на странице. Длинные посты с разрывом "читать далее" работают лучше. Видео с субтитрами — охват ×3. Клипы < 60 секунд — отдельная лента.

МАРКИРОВКА РЕКЛАМЫ (закон РФ, обязательно для рекламных постов): пометка "Реклама", указание рекламодателя, токен ERID через ОРД. Указываю что нужно промаркировать и как.

КОНТЕНТ-ПЛАН на 30 дней: рубрики с частотой → конкретные темы → форматы → оптимальное время публикации для RU-аудитории.

Спрашиваю: ниша, продукт, текущая аудитория, цель канала (охват / продажи / личный бренд).`,
  },
  {
    id: "ads",
    icon: "🎯",
    title: "Реклама",
    title_en: "Ad Creative",
    desc: "VK Ads, Яндекс Директ — тексты и аудитории",
    prompt: `Ты специалист по платному трафику на RU-рынке. CTR в VK Ads хороший от 0.8%, в Яндекс Директе — от 5% на поиске.

МЕТОДОЛОГИЯ:
— HOPKINS (прямое применение): "advertising is salesmanship in print". Каждое объявление — продавец, который ДОКАЗЫВАЕТ. "Качественно", "профессионально", "лучший" — ЗАПРЕЩЕНЫ. Только цифры, сроки, сравнения.
— SCHWARTZ: определяешь уровень аудитории в канале. Холодная аудитория VK/РСЯ = уровень 1-2 (нужна история или боль). Горячий поиск Директа = уровень 4-5 (сразу оффер и цена).
— OGILVY: заголовок тестируется в 3 вариантах. Использование magnetic words: "как", "бесплатно", "новый", "раскрытый".

VK ADS: Универсальная запись, реклама сайта, лид-форма. Первые 2 строки до "читать далее" решают CTR. Формула: боль → решение → доказательство → CTA с дедлайном.

ЯНДЕКС ДИРЕКТ (поиск): Заголовок 1 (56 симв) — ключ + оффер. Заголовок 2 (30 симв) — выгода или цифра. Текст (81 симв) — доказательство + CTA. Минус-слова обязательны.

РСЯ: как таргет — визуальный крючок + короткий текст. Тестируй 3+ креатива.

Для каждого канала: 3 варианта объявления + аудитория + примерный CPL + что тестировать первым.`,
  },
  {
    id: "seo",
    icon: "🔍",
    title: "SEO / Яндекс / GEO",
    title_en: "SEO & GEO",
    desc: "Позиции в Google, Яндексе и AI-поиске",
    prompt: `Ты SEO и GEO стратег. Знаешь разницу: Яндекс весит поведенческие факторы больше ссылок. Google в 2025 ранжирует E-E-A-T и helpful content. GEO (Generative Engine Optimization) — новое: как попасть в ответы ChatGPT, Perplexity, Google AI Overview.

ЯНДЕКС SEO: Поведенческие факторы — время на сайте, отказы, CTR в выдаче. Тексты для Яндекса: LSI-ключи, структура с H2-H3, факты и цифры. ИКС (индекс качества сайта) — следим.

GOOGLE SEO: E-E-A-T: Experience (личный опыт), Expertise (экспертиза), Authoritativeness (авторитет), Trust (доверие). Структура: главный ключ в title + H1, LSI в подзаголовках, FAQ-блок для People Also Ask.

GEO-ОПТИМИЗАЦИЯ: Чтобы попасть в AI Overview и ответы ботов: 1) Чёткие определения в начале абзацев 2) Структурированные списки (AI их цитирует) 3) FAQ с прямыми ответами 4) Schema markup (FAQ, HowTo, Article) 5) Упоминания бренда на авторитетных сайтах.

МЕТОДОЛОГИЯ:
— OGILVY для title/H1: 80% трафика решает заголовок поиска. named-audience + цифра + результат. "Стоматолог на Покровке от 2000 ₽" > "Стоматология Москва".
— HOPKINS для контента: E-E-A-T = верифицируемость. Цифры, скриншоты, кейсы ранжируются выше размытых текстов. GEO работает так же — AI цитирует именно числа и списки, не лирику.
— SCHWARTZ для структуры статей: коммерческий запрос (Most Aware) → сразу оффер + цена + гарантия в первых 200 словах. Информационный (Problem Aware) → проблема → причины → решение. Разные H1 под разные уровни.

Спрашиваю: URL или тема, рынок (RU/EN), приоритет (Яндекс / Google / AI-поиск).`,
  },
  {
    id: "competitors",
    icon: "🔎",
    title: "Конкуренты",
    title_en: "Competitor Intel",
    desc: "Разведка по 7 пунктам: что скопировать, что избежать",
    prompt: `Ты аналитик конкурентов. Задача — не описать конкурента, а найти возможности для дифференциации.

МЕТОДОЛОГИЯ:
— OGILVY: "изучай рекламу конкурентов — это самый дешёвый способ узнать что работает". Не копируешь — видишь паттерны.
— HOPKINS: "слабые места → твоя возможность". Негативные отзывы конкурентов = карта для твоего позиционирования.
— SCHWARTZ: определяешь на каком уровне осведомлённости конкурент бьёт рынок. Если все на уровне 3-4 (sophisticated market), можно выиграть перейдя на уровень 5 (identity) или вернувшись к 2 (боль).


РАЗВЕДКА по 7 пунктам: 1) Позиционирование и USP — как они описывают себя, в чём их главное обещание 2) Целевая аудитория — кому продают, кого НЕ берут 3) Каналы трафика — SEO, платный трафик, соцсети, партнёрки (по данным SimilarWeb/SpyFu) 4) Контент-стратегия — темы, форматы, частота, что работает лучше всего 5) Продукты и цены — линейка, тарифы, что продают в апсейле 6) Слабые места — что говорят в отзывах на Отзовик, Google, App Store, TrustPilot (негатив — это твоя возможность) 7) Стратегия: что скопировать (работает у них → сработает у тебя), что избежать (их слабость → твоё преимущество).

ВЫВОД: формулирую позицию "мы для тех, кто разочарован в [конкурент] потому что..."

Попроси ссылку на конкурента или описание. Если дашь несколько — сравню в таблице.`,
  },
  {
    id: "funnel",
    icon: "🔄",
    title: "Воронка продаж",
    title_en: "Sales Funnel",
    desc: "CJM, точки потери, как поднять конверсию",
    prompt: `Ты специалист по воронкам продаж для малого и среднего бизнеса. Знаешь: средняя воронка теряет 68% лидов между заявкой и оплатой — и в большинстве случаев проблема не в продукте, а в переходах между этапами.

МЕТОДОЛОГИЯ воронки:
— SCHWARTZ: каждый этап = свой уровень осведомлённости. Трафик=1 (Unaware). Лид=2 (Problem). Квал=3 (Solution). КП=4 (Product). Оплата=5 (Most Aware). Один сломанный "переход уровня" рушит весь поток.
— HOPKINS: "68% теряется между заявкой и оплатой" — не абстракция, каждую воронку измеряй реальными цифрами клиента. Без цифр — не воронка, а схема.
— OGILVY в point of conversion: любой CTA = обещание + доказательство + срочность. "Получить КП" без обещания — мёртвый CTA.

ДИАГНОСТИКА воронки (с чего начинаю): Какой этап самый "дырявый"? Считаем конверсию каждого перехода: трафик → лид → квал → встреча → КП → оплата. Один слабый переход = весь рост.

ПРОЕКТИРОВАНИЕ: Для каждого этапа: 1) Что должен почувствовать клиент 2) Какой контент/действие переводит на следующий шаг 3) Что мешает переходу (возражение или трение).

ИНСТРУМЕНТЫ по этапам: Верх воронки — лид-магнит, SEO, реклама. Середина — email-nurture, кейсы, вебинар. Низ — личная встреча, пробный период, дедлайн.

RU-специфика: B2B в России — решение принимает несколько человек, сделка 2-6 недель. Нужен nurture-контент для "комитета закупки", а не только для ЛПР.

Спрашиваю: продукт, средний чек, цикл сделки, где сейчас теряете больше всего?`,
  },
  {
    id: "launch",
    icon: "🚀",
    title: "Запуск продукта",
    title_en: "Product Launch",
    desc: "Плейбук запуска: предзапуск → запуск → рост",
    prompt: `Ты стратег запусков. 80% провалившихся запусков провалились на предзапуске — аудитория не разогрета.

МЕТОДОЛОГИЯ:
— SCHWARTZ (центральная роль): запуск = провести аудиторию через 5 уровней осведомлённости за короткий срок. Предзапуск 1-2 (Unaware→Problem), запуск 3-4 (Solution→Product), закрытие 5 (Most Aware → покупка). Попытка продать на 1-м уровне = провал.
— HOPKINS: каждое письмо/пост запуска содержит число. "Ранний доступ для первых 100" > "Ранний доступ".
— OGILVY: темы писем запуска — заголовки для конкретной аудитории. Для B2B "Для владельцев таксопарков на 500+ машин..." > "Запуск нового продукта".


ПЛЕЙБУК (3 фазы):

ПРЕДЗАПУСК (2-4 недели до): Цель — создать ажиотаж и собрать список ожидания. Действия: тизер без раскрытия продукта → проблемный контент (почему существующие решения не работают) → вовлечение аудитории (опрос, ранний доступ) → социальное доказательство от бета-пользователей. KPI: размер листа ожидания.

ЗАПУСК (3-7 дней): День 1 — открытие + полное раскрытие + бонус первым N покупателям. День 2-5 — контент закрывающий возражения (FAQ, кейсы, разбор "для кого"). Последние 24ч — дедлайн + "это закроется". KPI: конверсия листа ожидания в покупки.

ПОСТ-ЗАПУСК (2 недели): Онбординг первых клиентов → сбор отзывов → кейсы → evergreen воронка.

RU + EN специфика: для RU — Telegram-канал как основной канал предзапуска; для EN — email-лист + LinkedIn.

Спрашиваю: что запускаем, дата запуска, текущая аудитория, бюджет.`,
  },
  {
    id: "brand",
    icon: "💼",
    title: "Бренд и голос",
    title_en: "Brand Voice",
    desc: "Tone of voice, позиционирование, LinkedIn/TenChat",
    prompt: `Ты стратег личного и корпоративного бренда. Работаешь по Ogilvy: "Каждая реклама — долгосрочная инвестиция в образ бренда. Большинство рекламодателей тратят жизнь на убийство своего бренда ради quarterly sales." Голос бренда — долгосрочное обещание поведения, а не креатив одного поста.

МЕТОДОЛОГИЯ:
— OGILVY (основа): каждое утверждение о бренде = верифицируемое. Никаких "качественный", "надёжный".
— SCHWARTZ для голоса: определяет уровень рынка. На sophisticated-рынке (4-5) прямые утверждения мертвы — голос должен сигналить identity ("бренд для тех кто..."), а не benefit.
— HOPKINS для личного бренда: каждый пост содержит конкретный факт из твоей практики. Цифра клиента, имя проекта, точная дата. "Помогаем бизнесу" = мёртвый голос. "За 2024 вытащил 14 из 15 таксопарков из убытка" = живой.

ГОЛОС БРЕНДА (Brand Voice): Определяю по 4 осям: Формальный ↔ Разговорный, Серьёзный ↔ С юмором, Экспертный ↔ Дружеский, Провокационный ↔ Осторожный. Для каждого голоса: примеры фраз которые ГОВОРИШЬ и которые НИКОГДА не говоришь.

ЛИЧНЫЙ БРЕНД: Формула поста для LinkedIn/TenChat по структуре: Хук (первые 2 строки обрываются — заставляют нажать "ещё") → История (личный опыт, конкретная ситуация) → Вывод (одна ключевая мысль) → Вопрос аудитории. Нет общих советов, только личный опыт с конкретикой.

ПОЗИЦИОНИРОВАНИЕ личного бренда: "Я помогаю [кому] достичь [что] через [как] даже если [возражение]". Затем — 3 темы-столпа на которые говоришь постоянно.

RU-специфика: TenChat — деловая сеть, там ранжируются посты с профессиональной экспертизой. LinkedIn — EN-аудитория, работает хук + личная история.

Спрашиваю: чем занимаешься, целевая аудитория, текущий образ в сети, что хочешь изменить?`,
  },
  {
    id: "newsletter",
    icon: "📰",
    title: "Рассылка",
    title_en: "Newsletter",
    desc: "Email-рассылка которую ждут и читают",
    prompt: `Ты стратег контентных рассылок. Знаешь: хорошая рассылка — это не "контент-маркетинг", это отношения. Люди подписываются на человека, а не на тему.

МЕТОДОЛОГИЯ:
— SCHWARTZ: список рассылки = уже Problem/Solution Aware (раз подписались). Не продавай продукт — продавай механизм. Письмо с прямой продажей = отписка.
— HOPKINS для тем: specific > general. Тема письма = продавец в inbox. "Как я поднял конверсию на 34%" > "Советы по конверсии".
— OGILVY для прехедера: продолжает тему с новым конкретным фактом, а не парафразом.

9 ФОРМАТОВ рассылок (подбираю под цель): 1) Curated — лучшее за неделю с твоим комментарием 2) Story-driven — личная история + вывод + применение 3) One Big Idea — одна мысль раскрытая полностью 4) Case Study — разбор одного кейса с цифрами 5) Framework — структура которую можно применить 6) Contra — спорное мнение против тренда 7) Behind the scenes — как ты работаешь изнутри 8) Q&A — ответы на вопросы подписчиков 9) Roundup — "что я читал/смотрел/использовал".

СТРУКТУРА выпуска: Тема письма (Hopkins: специфика > общее) → Прехедер (Ogilvy: новый факт, не парафраз) → Лид (одна строка) → Тело (один формат) → CTA (один).

RU-специфика: Substack набирает популярность в RU. Email-рассылки через Unisender, SendPulse. Telegram-канал как замена рассылке — частый RU-паттерн.

Спрашиваю: тема рассылки, периодичность, текущая база, цель (охват / продажи / сообщество)?`,
  },
  {
    id: "webdesign",
    icon: "🎨",
    title: "Дизайн сайта",
    title_en: "Website Design",
    desc: "Вставь описание — получи живой новый сайт",
    prompt: `Ты веб-дизайнер и копирайтер — работаешь в Студии, не в чате. Обычно пользователь вставляет URL и данные через форму. Этот промпт — fallback на случай прямого вопроса в чате: направь пользователя на 🎨 Студию сайта (форма слева).

ПЕРВЫЙ ШАГ в Студии — определи уровень осознанности ЦА по SCHWARTZ:
— B2C массовый товар → уровень 2-3: называй боль точнее конкурентов, покажи механизм
— B2B услуги → уровень 1-2: начинай с истории или провокации
— Инновационный продукт → уровень 1: лендинг строится как educational story

В 80% случаев для МСБ в РФ это уровень 2-3. Hero-блок меняется под уровень.

OGILVY для заголовка: name the audience + specific result + time frame. "Маркетинг для бизнеса" — мёртв. "Увеличиваем выручку ресторанов на 34% за 90 дней" — живёт.

HOPKINS для всех текстов: запрещены "качественно/профессионально/лучший". Только цифры и сравнения.`,
  },
  {
    id: "proposal",
    icon: "📋",
    title: "Предложение клиенту",
    title_en: "Client Proposal",
    desc: "КП которое читают и на которое соглашаются",
    prompt: `Ты специалист по коммерческим предложениям. 90% КП провальны — начинаются с "о нас", а не с боли клиента.

МЕТОДОЛОГИЯ:
— SCHWARTZ: до написания КП определи уровень ЛПР. Problem Aware → раскрой боль точнее чем он её видит. Solution Aware → покажи свой механизм отличающий от альтернатив. Product Aware → сними 3 главных возражения.
— OGILVY: каждое утверждение специфично. "Увеличим конверсию" → "Средний клиент вашей ниши видит +23-41% конверсии за 60 дней".
— HOPKINS: КП = продавец. Продавец не говорит "мы профи", он говорит "из 47 клиентов в нише 44 остались на 2-й год". Факт > обещание.


СТРУКТУРА КП которое работает: 1) Проблема клиента его словами — показываешь что понял ситуацию лучше чем он сам её описал 2) Почему сейчас — цена бездействия: что происходит если не решить 3) Твоё решение — не перечень услуг, а путь от А (сейчас) до Б (результат) 4) Как это работает — 3-4 шага процесса, никаких "чёрных ящиков" 5) Социальное доказательство — один кейс максимально релевантный их ситуации, с цифрами 6) Инвестиция — называй "инвестиция", не "стоимость". Обоснуй ROI. 7) Следующий шаг — один конкретный шаг, не "свяжитесь с нами".

ПО ОGILVY: каждое утверждение должно быть специфичным. Не "увеличим продажи", а "средний клиент в вашей нише видит рост конверсии на 23-41% за первые 60 дней".

RU-специфика: для B2B в России — формат PDF лучше онлайн-ссылки. Сроки оплаты и реквизиты в конце. Печать + подпись всё ещё важны для крупных компаний.

Спрашиваю: кто клиент, что предлагаешь, есть ли кейсы, бюджет клиента.`,
  },
];

const BUSINESS_TYPES: { id: string; label: string; icon: string; context: string }[] = [
  { id: "restaurant", label: "Ресторан / Кафе", icon: "🍽️", context: "RU-специфика HoReCa: средний чек, LTV, доставка через Яндекс.Еда/Delivery Club, резервы, отзывы на Яндекс.Картах критичны для SEO-позиций. ЦА — локальная, радиус 2-5 км. Каналы: Яндекс.Карты, VK, Telegram, Инстаграм (через VPN)." },
  { id: "beauty", label: "Салон красоты", icon: "💅", context: "Индустрия бьюти: мастера как бренд внутри бренда, YCLIENTS для записи, Instagram/VK для портфолио работ. Ключевые KPI: LTV, возврат клиента. Конкуренция за мастеров важнее чем за клиентов." },
  { id: "medical", label: "Медклиника", icon: "🩺", context: "Медицина RU: лицензирование обязательно, маркировка рекламы по закону о рекламе медуслуг (нужно предупреждение о противопоказаниях). Доверие через врачей-экспертов, а не через клинику. ПроДокторов, НаПоправку — ключевые отзовики." },
  { id: "shop", label: "Магазин / E-com", icon: "🛍️", context: "E-commerce RU: Wildberries/Ozon как основные маркетплейсы, средняя комиссия 20%, контентные карточки решают. Свой сайт для премиум-позиционирования. Яндекс.Маркет, Авито как дополнительные каналы." },
  { id: "b2b", label: "B2B услуги", icon: "💼", context: "B2B RU: длинный цикл сделки (2-6 недель), комитет закупки (3-5 ЛПР), решает ROI + доверие к эксперту. Каналы: LinkedIn (через VPN), TenChat, холодные письма через Apollo/Instantly, рефералы. ТЗ и тендеры 44/223-ФЗ для госсектора." },
  { id: "agency", label: "Агентство", icon: "🎯", context: "Маркетинговые/digital-агентства: ценятся кейсы с цифрами, личный бренд основателя, процессы. Конкуренция за таланты = за клиентов. Основные клиенты приходят по рекомендациям и с видеоконтента владельца." },
  { id: "edu", label: "Образование / Курсы", icon: "🎓", context: "EdTech RU: продажа через вебинары, аккредитация (нужна лицензия для некоторых форматов), Getcourse/Skillbox/Нетология как бенчмарки. CPL высокий, LTV решает. Сертификаты и карьерный результат — ключевые аргументы." },
  { id: "fitness", label: "Фитнес / Спорт", icon: "💪", context: "Фитнес RU: абонементная модель, retention критичен, тренеры как бренд (аналогично салонам красоты). Мобильные приложения + оффлайн. Упор на трансформацию до/после в контенте." },
  { id: "other", label: "Другое", icon: "🔹", context: "" },
];

const HANDOFFS: Record<string, string[]> = {
  positioning: ["copy", "landing", "brand"],
  copy: ["landing", "email", "ads"],
  leadmagnet: ["email", "landing", "funnel"],
  landing: ["ads", "seo", "funnel"],
  email: ["copy", "newsletter", "funnel"],
  atomizer: ["social", "newsletter"],
  social: ["ads", "atomizer", "brand"],
  ads: ["landing", "competitors", "funnel"],
  seo: ["landing", "copy"],
  competitors: ["positioning", "ads"],
  funnel: ["copy", "email", "landing"],
  launch: ["copy", "email", "ads"],
  brand: ["copy", "social", "positioning"],
  newsletter: ["copy", "atomizer"],
  webdesign: ["seo", "ads"],
  proposal: ["copy", "brand"],
};

const METHODOLOGY_SUFFIX = `

МЕТОДОЛОГИЧЕСКИЙ ФУНДАМЕНТ (применяй во всех ответах неявно):
— OGILVY: любое утверждение о продукте должно быть верифицируемым. Прилагательные "качественный", "лучший", "профессиональный" — запрещены. Заменяй на цифру, сравнение или цитату.
— SCHWARTZ: до ответа мысленно определи уровень осведомлённости аудитории (1-5). Для Unaware — история, для Most Aware — оффер и цена. Текст радикально меняется под уровень.
— HOPKINS: реклама = продавец в печати. Продавец не говорит "мы команда профессионалов", он говорит "из 847 клиентов 92% вернулись". Тот же стандарт.

ОБЯЗАТЕЛЬНЫЙ ФИНАЛЬНЫЙ БЛОК (точный формат, без отклонений):

---
**🧠 Почему я так сделал:**

• **Ogilvy** — [КОНКРЕТНОЕ правило: named-audience / magnetic word / sell-the-effect / headline 80/20 / proof-point — и где в моём ответе это проявилось]
• **Schwartz** — [какой уровень осведомлённости (1-5) я предположил и почему мой ответ подходит именно для него]
• **Hopkins** — [какую цифру/специфику я добавил вместо абстракции и какой альтернативы избежал]
• **Чего я НЕ делал и почему** — [один приём который сработал бы у ChatGPT, но дал бы шаблонный результат в этой задаче]

Каждый пункт ≤ 2 строк. Не пересказ — доказательство экспертизы.`;

const STUDIO_PROMPT = `Ты топовый веб-дизайнер и копирайтер. Создаёшь лендинги уровня $5000-агентства.

ПЕРВЫЙ МЫСЛЕННЫЙ ШАГ — уровень осознанности ЦА по SCHWARTZ:
— B2C массовый товар → уровень 2-3 (боль известна, решений много)
— B2B услуги → уровень 1-2 (проблема не сформулирована)
— Инновационный продукт → уровень 1 (Unaware)
В 80% случаев для МСБ в РФ это 2-3. Hero-блок адаптируется под уровень.

КРИТИЧЕСКИ ВАЖНО: твой ответ — ТОЛЬКО валидный HTML начиная с <!DOCTYPE html>. Никакого текста до или после. Никаких markdown-блоков \`\`\`.

ВИЗУАЛЫ — КРИТИЧНО (source.unsplash.com мёртв, НЕ используй его):
Вариант A: градиентный фон CSS без изображения (безопасный дефолт):
  background: linear-gradient(135deg, var(--accent-dark) 0%, var(--accent) 100%);
Вариант B: если точно знаешь ID фото на images.unsplash.com, используй формат:
  https://images.unsplash.com/photo-XXXXXXXXX?w=1600&q=80
Если не уверен в ID — используй Вариант A. Лучше красивый градиент, чем broken-image.

ДИЗАЙН (premium уровень):
— Google Fonts через @import: Playfair Display для заголовков, Inter для текста
— CSS анимации: классы fadeInUp на секциях через Intersection Observer в <script>
— Hero: полноэкранный (min-height: 100vh), тёмный градиент-оверлей, заголовок белый крупный font-size: clamp(2.2rem,5vw,4rem)
— Карточки: белые с тенью box-shadow, hover: transform translateY(-6px) + усиленная тень
— Кнопки CTA: градиент, hover: яркий + translateY(-2px), border-radius: 50px
— Цвета: подбери под нишу, НЕ синий Bootstrap, НЕ стандартный зелёный
— Mobile responsive: @media (max-width: 768px) для всех секций
— Числа и факты крупно: 3 статистики в hero (например "200+ клиентов", "5 лет", "98% довольны")

СТРУКТУРА ОБЯЗАТЕЛЬНО (все секции):
1. Nav — логотип + телефон + кнопка CTA
2. Hero — заголовок + подзаголовок + 2 кнопки + 3 цифры-статистики
3. Боль — "Узнаёте себя?" — 3 болевых пункта с emoji
4. Решение — что делаем + механизм
5. Преимущества — 3 карточки с иконкой-emoji, заголовком, описанием
6. Отзывы — ИСПОЛЬЗУЙ РЕАЛЬНЫЕ ОТЗЫВЫ ДОСЛОВНО если даны, иначе придумай 3 реалистичных с именем и ⭐⭐⭐⭐⭐
7. Процесс — 3-4 шага "Как мы работаем"
8. CTA финальный — заголовок + кнопка + "Или позвоните: ..."
9. Footer — логотип + навигация + копирайт

КОПИРАЙТИНГ (Ogilvy + Hopkins СТРОГО):
— Hero заголовок: конкретный результат за конкретное время ("Увеличиваем выручку московских ресторанов на 34% за 90 дней")
— Ноль абстракций: "качественно", "профессионально", "лучший" — ЗАПРЕЩЕНО
— Только цифры, факты, конкретика
— CTA: глагол + выгода + срочность ("Получить бесплатный аудит — осталось 3 места")`;

const STUDIO_QUOTES: { author: string; role: string; text: string }[] = [
  { role: "Schwartz · анализ ЦА", author: "Eugene Schwartz", text: "Прежде чем писать, мы должны знать, к кому пишем." },
  { role: "Ogilvy · заголовок", author: "David Ogilvy", text: "Заголовок — это 80% бюджета вашей рекламы." },
  { role: "Ogilvy · первое впечатление", author: "David Ogilvy", text: "Первое впечатление — единственное." },
  { role: "Hopkins · специфика", author: "Claude Hopkins", text: "Реклама — это продажа в печати. Больше ничего." },
  { role: "Финал", author: "Natalia Brovkina", text: "Готово. 16 агентов. Одна методология." },
];

type Message = { role: "user" | "assistant"; content: string };

export default function Home() {
  const [activeAgent, setActiveAgent] = useState<typeof AGENTS[0] | null>(null);
  const [messages, setMessages] = useState<Message[]>([]);
  const [input, setInput] = useState("");
  const [loading, setLoading] = useState(false);
  const [apiKey, setApiKey] = useState<string | null>(null);
  const [keyInput, setKeyInput] = useState("");
  const [keyError, setKeyError] = useState("");
  const [previewHtml, setPreviewHtml] = useState<string | null>(null);
  const [studioName, setStudioName] = useState("");
  const [studioDesc, setStudioDesc] = useState("");
  const [studioReviews, setStudioReviews] = useState("");
  const [studioUrl, setStudioUrl] = useState("");
  const [studioLoading, setStudioLoading] = useState(false);
  const [studioStep, setStudioStep] = useState(0);
  const [originalSiteUrl, setOriginalSiteUrl] = useState("");

  // Brand Memory
  const [brand, setBrand] = useState({ type: "", name: "", what: "", who: "", usp: "", voice: "", cases: "", competitors: "" });
  const [showBrand, setShowBrand] = useState(false);

  // Demo mode — hides sidebar, expands iframe to full width
  const [demoMode, setIsDemoMode] = useState(false);

  // Team Mode
  const [teamMode, setTeamMode] = useState(false);
  const [teamTask, setTeamTask] = useState("");
  const [teamSelected, setTeamSelected] = useState<string[]>(["positioning", "copy", "landing", "ads"]);
  const [teamLoading, setTeamLoading] = useState(false);
  const [teamResults, setTeamResults] = useState<{ agentId: string; content: string }[]>([]);

  const runTeam = async () => {
    if (!teamTask.trim() || teamSelected.length === 0) return;
    setTeamLoading(true);
    setTeamResults([]);
    const agents = teamSelected.map(id => AGENTS.find(a => a.id === id)).filter(Boolean) as typeof AGENTS;
    const results = await Promise.all(
      agents.map(async (agent) => {
        try {
          const res = await fetch("/api/chat", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({
              messages: [{ role: "user", content: teamTask }],
              systemPrompt: agent.prompt + METHODOLOGY_SUFFIX + brandContext(),
              apiKey,
            }),
          });
          const data = await res.json();
          return { agentId: agent.id, content: data.content as string };
        } catch {
          return { agentId: agent.id, content: "Ошибка запроса" };
        }
      })
    );
    setTeamResults(results);
    setTeamLoading(false);
  };

  useEffect(() => {
    const stored = typeof window !== "undefined" ? localStorage.getItem("anthropic_api_key") : null;
    if (stored) {
      queueMicrotask(() => setApiKey(stored));
    }
    const storedBrand = typeof window !== "undefined" ? localStorage.getItem("brand_memory") : null;
    if (storedBrand) {
      try {
        const parsed = JSON.parse(storedBrand);
        queueMicrotask(() => setBrand(parsed));
      } catch {}
    }
    // Demo mode for video fallback
    const params = new URLSearchParams(window.location.search);
    const demoId = params.get("demo");
    if (demoId) {
      const demoMap: Record<string, { url: string; file: string }> = {
        simbios: { url: "https://simbios-marketing.ru", file: "/demo/simbios-after.html" },
        medea: { url: "http://medeadent.tilda.ws", file: "/demo/medea-after.html" },
        backup1: { url: "https://example.com", file: "/demo/backup1-after.html" },
        backup2: { url: "https://example.com", file: "/demo/backup2-after.html" },
      };
      const demo = demoMap[demoId];
      if (demo) {
        fetch(demo.file)
          .then(r => r.ok ? r.text() : Promise.reject())
          .then(html => {
            const webdesign = AGENTS.find(a => a.id === "webdesign");
            if (webdesign) setActiveAgent(webdesign);
            setPreviewHtml(html);
            setOriginalSiteUrl(demo.url);
            setIsDemoMode(true);
          })
          .catch(() => {});
      }
    }
  }, []);

  const saveBrand = () => {
    localStorage.setItem("brand_memory", JSON.stringify(brand));
    setShowBrand(false);
  };

  const brandContext = () => {
    const typeInfo = BUSINESS_TYPES.find(t => t.id === brand.type);
    const filled = [
      typeInfo && typeInfo.id !== "other" && `Тип бизнеса: ${typeInfo.label}`,
      brand.name && `Название: ${brand.name}`,
      brand.what && `Что продаём: ${brand.what}`,
      brand.who && `Кому продаём: ${brand.who}`,
      brand.usp && `Чем отличаемся: ${brand.usp}`,
      brand.voice && `Тон голоса: ${brand.voice}`,
      brand.cases && `Кейсы с цифрами: ${brand.cases}`,
      brand.competitors && `Конкуренты: ${brand.competitors}`,
    ].filter(Boolean);
    if (!filled.length) return "";
    const verticalKnowledge = typeInfo?.context ? `\n\nВЕРТИКАЛЬНАЯ ЭКСПЕРТИЗА: ${typeInfo.context}` : "";
    return `\n\n=== КОНТЕКСТ БИЗНЕСА (используй во всех ответах) ===\n${filled.join("\n")}${verticalKnowledge}\n=== /КОНТЕКСТ ===`;
  };

  const brandFilled = Object.values(brand).some(v => v.trim().length > 0);

  const saveKey = () => {
    const key = keyInput.trim();
    if (!key.startsWith("sk-ant-")) {
      setKeyError("Ключ должен начинаться с sk-ant-");
      return;
    }
    localStorage.setItem("anthropic_api_key", key);
    setApiKey(key);
    setKeyError("");
  };

  const resetKey = () => {
    localStorage.removeItem("anthropic_api_key");
    setApiKey(null);
    setKeyInput("");
  };

  const selectAgent = (agent: typeof AGENTS[0]) => {
    setActiveAgent(agent);
    setMessages([{ role: "assistant", content: `Привет! Я ${agent.title}. ${agent.desc}. Чем могу помочь?` }]);
    setInput("");
  };

  const handoffToAgent = (targetAgentId: string) => {
    const target = AGENTS.find(a => a.id === targetAgentId);
    if (!target || !activeAgent) return;
    const lastAssistant = [...messages].reverse().find(m => m.role === "assistant");
    const context = lastAssistant?.content.split(/\n---\n/)[0]?.trim() || "";
    const fromAgentTitle = activeAgent.title;
    setActiveAgent(target);
    setMessages([
      { role: "assistant", content: `Привет! Я ${target.title}. Получил(а) контекст от ${fromAgentTitle} — применю свою экспертизу.` },
    ]);
    setInput(`Контекст от ${fromAgentTitle}:\n\n${context}\n\nПродолжи работу — примени свою экспертизу.`);
  };

  const sendMessage = async () => {
    if (!input.trim() || !activeAgent || loading) return;
    const userMsg: Message = { role: "user", content: input };
    const newMessages = [...messages, userMsg];
    setMessages(newMessages);
    setInput("");
    setLoading(true);
    try {
      const res = await fetch("/api/chat", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ messages: newMessages, systemPrompt: activeAgent.prompt + METHODOLOGY_SUFFIX + brandContext(), apiKey }),
      });
      const data = await res.json();
      const content = data.content as string;
      setMessages([...newMessages, { role: "assistant", content }]);
      if (content.includes("<!DOCTYPE html") || content.includes("<html")) {
        setPreviewHtml(content);
      }
    } catch {
      setMessages([...newMessages, { role: "assistant", content: "Ошибка. Попробуй ещё раз." }]);
    }
    setLoading(false);
  };

  const runStudio = async () => {
    if (!studioName && !studioDesc && !studioUrl) return;
    setStudioLoading(true);
    setStudioStep(0);
    setPreviewHtml(null);
    if (studioUrl) setOriginalSiteUrl(studioUrl);
    const stepTimer = setInterval(() => setStudioStep(s => Math.min(s + 1, 3)), 4000);
    const userContent = [
      studioUrl && `URL их текущего сайта: ${studioUrl}`,
      studioName && `Название бизнеса: ${studioName}`,
      studioDesc && `Описание: ${studioDesc}`,
      studioReviews && `Реальные отзывы клиентов (используй ДОСЛОВНО в секции отзывов с именем и ⭐⭐⭐⭐⭐):\n${studioReviews}`,
      studioUrl && `\nИспользуй содержимое их сайта как основу: сохрани услуги, контакты, команду, но сделай ПРАВИЛЬНУЮ упаковку — заголовок по Ogilvy, социальные доказательства, чёткий CTA.`,
    ].filter(Boolean).join("\n\n");
    try {
      const res = await fetch("/api/chat", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          messages: [{ role: "user", content: userContent }],
          systemPrompt: STUDIO_PROMPT + brandContext(),
          apiKey,
          scrapeUrl: studioUrl || undefined,
          maxTokens: 16384,
        }),
      });
      const data = await res.json();
      clearInterval(stepTimer);
      setStudioStep(4);
      let html = data.content as string;
      if (html.includes("```")) html = html.replace(/^```html?\n?/i, "").replace(/\n?```$/,"");
      setPreviewHtml(html);
    } catch {
      clearInterval(stepTimer);
    } finally {
      setStudioLoading(false);
    }
  };

  const addFooter = (html: string): string => {
    const footer = `
<div style="text-align:center;padding:14px 20px;background:#0D1B2A;color:#C8B79C;font-family:system-ui,-apple-system,sans-serif;font-size:12px;letter-spacing:1px;border-top:1px solid rgba(200,183,156,0.2)">
  Сделано через <a href="https://natashabrovkina.com?utm_source=export" style="color:#C8B79C;text-decoration:none;border-bottom:1px dotted #C8B79C" target="_blank" rel="noopener">natashabrovkina.com</a> — AI-команда маркетологов из 16 специалистов за 2&nbsp;990&nbsp;₽/мес
</div>`;
    if (html.includes("</body>")) return html.replace("</body>", `${footer}</body>`);
    return html + footer;
  };

  const downloadHtml = () => {
    if (!previewHtml) return;
    const blob = new Blob([addFooter(previewHtml)], { type: "text/html" });
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url; a.download = `${studioName || "website"}.html`; a.click();
    URL.revokeObjectURL(url);
  };

  const openFullscreen = () => {
    if (!previewHtml) return;
    const blob = new Blob([addFooter(previewHtml)], { type: "text/html" });
    const url = URL.createObjectURL(blob);
    window.open(url, "_blank");
  };

  // BYOK modal state
  const [showKeyModal, setShowKeyModal] = useState(false);

  return (
    <div
      data-palette="navy"
      className="h-screen overflow-hidden flex flex-col"
      style={{ background: "var(--cream)" }}
    >
      {/* Header — unified with SiteNav visual language */}
      <header
        style={{
          background: "var(--navy)",
          borderBottom: "1px solid rgba(245,243,239,0.08)",
        }}
        className="px-4 md:px-8 py-4 flex flex-wrap items-center justify-between gap-3"
      >
        <Link
          href="/"
          style={{ display: "flex", alignItems: "center", gap: 14, textDecoration: "none", color: "inherit", flexShrink: 0 }}
        >
          <div
            style={{
              width: 40,
              height: 40,
              borderRadius: "50%",
              background: "url('/assets/portrait.png') center 15%/cover, var(--navy-2)",
              border: "2px solid var(--gold)",
              flexShrink: 0,
            }}
          />
          <div style={{ fontFamily: "var(--font-serif)", fontSize: 21, fontWeight: 600, lineHeight: 1.1, color: "var(--cream)" }}>
            natalia
            <small
              style={{
                display: "block",
                fontFamily: "var(--font-sans)",
                fontSize: 11,
                color: "rgba(245,243,239,0.55)",
                fontWeight: 500,
                letterSpacing: "0.12em",
                textTransform: "uppercase",
                marginTop: -2,
              }}
            >
              AI · marketing studio
            </small>
          </div>
        </Link>
        <div className="flex items-center gap-2 md:gap-4 flex-wrap">
          <Link href="/gallery" className="hidden md:block" style={{ color: "var(--gold)", fontSize: 11, letterSpacing: 1, textDecoration: "none" }}>← галерея</Link>
          <Link href="/method" className="hidden lg:block" style={{ color: "var(--gold)", fontSize: 11, letterSpacing: 1, textDecoration: "none" }}>метод</Link>
          <button
            onClick={() => { setTeamMode(!teamMode); setActiveAgent(null); }}
            style={{
              color: teamMode ? "var(--navy)" : "var(--gold)",
              fontSize: 11,
              letterSpacing: 1,
              backgroundColor: teamMode ? "var(--gold)" : "transparent",
              border: "1px solid var(--gold)",
              borderRadius: 4,
              padding: "4px 10px",
              cursor: "pointer",
              fontWeight: 600,
            }}
            title="Задай одну задачу — несколько специалистов ответят параллельно"
          >
            {teamMode ? "выйти из команды" : "команда"}
          </button>
          <button
            onClick={() => setShowBrand(true)}
            style={{
              color: brandFilled ? "var(--gold)" : "var(--steel)",
              fontSize: 11,
              letterSpacing: 1,
              background: brandFilled ? "rgba(200,183,156,0.1)" : "none",
              border: brandFilled ? "1px solid #C8B79C" : "1px solid transparent",
              borderRadius: 4,
              padding: "4px 10px",
              cursor: "pointer"
            }}
            title="Профиль бизнеса — все агенты его знают"
          >
            {brandFilled ? (brand.name || "мой бизнес") : "заполнить про бизнес"}
          </button>
          <button
            onClick={() => setShowKeyModal(true)}
            style={{ color: "var(--steel)", fontSize: 11, letterSpacing: 1, background: "none", border: "none", cursor: "pointer" }}
            title={apiKey ? "Свой API-ключ подключён" : "Использовать свой API-ключ"}
          >
            {apiKey ? "свой ключ" : "ключ"}
          </button>
        </div>
      </header>

      {/* Gold divider */}
      <div style={{ height: 1, background: "var(--gold)" }} />

      <main className={`flex flex-col md:flex-row flex-1 min-h-0 ${demoMode ? "max-w-7xl" : "max-w-5xl"} mx-auto w-full px-3 md:px-6 py-4 md:py-8 gap-3 md:gap-6`}>
        {/* Left: Agents */}
        {!demoMode && (
        <aside className="w-full md:w-72 flex-shrink-0 md:overflow-y-auto">
          <h2 style={{ color: "var(--navy)", fontFamily: "var(--font-serif)", fontSize: 18, fontWeight: 700, marginBottom: 6 }} className="hidden md:block">
            Ваша AI-команда
          </h2>
          <p style={{ color: "var(--steel)", fontSize: 12, marginBottom: 20, letterSpacing: 0.5 }} className="hidden md:block">YOUR AI MARKETING TEAM · ON DEMAND</p>
          <div className="flex flex-row md:flex-col gap-2 md:gap-3 overflow-x-auto md:overflow-x-visible pb-2 md:pb-0 -mx-3 md:mx-0 px-3 md:px-0">
            {AGENTS.map((agent) => (
              <button
                key={agent.id}
                onClick={() => selectAgent(agent)}
                className="text-left p-4 rounded-lg border transition-all"
                style={{
                  backgroundColor: activeAgent?.id === agent.id ? "var(--navy)" : "#fff",
                  borderColor: activeAgent?.id === agent.id ? "var(--gold)" : "#e5e0d8",
                  color: activeAgent?.id === agent.id ? "var(--cream)" : "var(--navy)",
                }}
              >
                <div className="flex items-center gap-2 mb-1">
                  <span style={{ display: "inline-flex", alignItems: "center", color: activeAgent?.id === agent.id ? "var(--gold)" : "var(--navy)" }}>{AGENT_ICONS[agent.id]}</span>
                  <span style={{ fontWeight: 600, fontSize: 14 }}>{agent.title}</span>
                </div>
                <p style={{ fontSize: 12, color: activeAgent?.id === agent.id ? "var(--gold)" : "var(--steel)" }}>{agent.desc}</p>
              </button>
            ))}
          </div>
        </aside>
        )}

        {/* Right panel */}
        <div className="flex-1 flex flex-col overflow-hidden">

          {/* ── TEAM MODE ── */}
          {teamMode && (
            <div className="flex-1 flex flex-col rounded-lg border overflow-hidden" style={{ borderColor: "var(--gold)", backgroundColor: "#fff" }}>
              <div className="px-6 py-4 border-b flex items-center gap-3" style={{ borderColor: "var(--gold)", background: "var(--navy)" }}>
                <span style={{ fontSize: 22 }}>🎯</span>
                <div>
                  <div style={{ fontWeight: 700, color: "var(--gold)", fontSize: 15 }}>Режим команды</div>
                  <div style={{ fontSize: 11, color: "#B8C2CF" }}>Задай одну задачу — выбранные специалисты ответят параллельно</div>
                </div>
              </div>
              <div className="flex-1 overflow-y-auto p-6 flex flex-col gap-5">
                <div>
                  <label style={{ fontSize: 11, fontWeight: 700, color: "var(--navy)", display: "block", marginBottom: 6, letterSpacing: 1 }}>ЗАДАЧА ДЛЯ КОМАНДЫ</label>
                  <textarea
                    className="w-full px-4 py-3 rounded-lg border text-sm outline-none resize-none"
                    style={{ borderColor: "#e5e0d8", background: "var(--cream)", color: "var(--navy)" }}
                    rows={3}
                    placeholder="Например: У нас запуск нового продукта через 2 недели. Как позиционироваться, что писать, куда пускать трафик?"
                    value={teamTask}
                    onChange={(e) => setTeamTask(e.target.value)}
                  />
                </div>
                <div>
                  <label style={{ fontSize: 11, fontWeight: 700, color: "var(--navy)", display: "block", marginBottom: 8, letterSpacing: 1 }}>КТО ОТВЕЧАЕТ <span style={{ color: "var(--steel)", fontWeight: 400 }}>(выбрано: {teamSelected.length})</span></label>
                  <div className="grid grid-cols-2 gap-2">
                    {AGENTS.filter(a => a.id !== "webdesign").map(a => {
                      const checked = teamSelected.includes(a.id);
                      return (
                        <button
                          key={a.id}
                          onClick={() => setTeamSelected(checked ? teamSelected.filter(id => id !== a.id) : [...teamSelected, a.id])}
                          className="text-left px-3 py-2 rounded-lg border flex items-center gap-2 transition-all"
                          style={{
                            backgroundColor: checked ? "var(--navy)" : "var(--cream)",
                            borderColor: checked ? "var(--gold)" : "#e5e0d8",
                            color: checked ? "var(--gold)" : "var(--navy)",
                          }}
                        >
                          <span style={{ display: "inline-flex", color: teamSelected.includes(a.id) ? "var(--gold)" : "var(--navy)" }}>{AGENT_ICONS[a.id]}</span>
                          <span style={{ fontSize: 12, fontWeight: 600 }}>{a.title}</span>
                          {checked && <span style={{ marginLeft: "auto", color: "var(--gold)" }}>✓</span>}
                        </button>
                      );
                    })}
                  </div>
                </div>

                {teamResults.length > 0 && (
                  <div className="flex flex-col gap-4 mt-4">
                    <div style={{ fontSize: 11, fontWeight: 700, color: "var(--gold)", letterSpacing: 1 }}>ОТВЕТЫ КОМАНДЫ</div>
                    {teamResults.map((r, i) => {
                      const agent = AGENTS.find(a => a.id === r.agentId);
                      if (!agent) return null;
                      const splitMatch = r.content.split(/\n---\n\s*\*\*🧠/i);
                      const mainText = splitMatch[0].trim();
                      const methodology = splitMatch[1] ? splitMatch[1].replace(/^\s*Почему я так сделал:\*\*\s*/i, "").trim() : null;
                      return (
                        <div key={i} className="rounded-xl border overflow-hidden" style={{ borderColor: "#e5e0d8" }}>
                          <div className="px-4 py-3 flex items-center gap-2" style={{ background: "var(--cream)", borderBottom: "1px solid #e5e0d8" }}>
                            <span style={{ display: "inline-flex", color: "var(--navy)" }}>{AGENT_ICONS[agent.id]}</span>
                            <span style={{ fontWeight: 700, color: "var(--navy)", fontSize: 13 }}>{agent.title}</span>
                          </div>
                          <div className="p-4 text-sm leading-relaxed whitespace-pre-wrap" style={{ color: "var(--navy)" }}>{mainText}</div>
                          {methodology && (
                            <div className="px-4 py-3 text-xs whitespace-pre-wrap" style={{ backgroundColor: "rgba(200,183,156,0.12)", borderTop: "1px solid var(--gold)", color: "var(--navy)" }}>
                              <div style={{ fontWeight: 700, color: "var(--gold)", marginBottom: 4, letterSpacing: 0.5, fontSize: 10, textTransform: "uppercase" }}>🧠 Методология</div>
                              {methodology}
                            </div>
                          )}
                        </div>
                      );
                    })}
                  </div>
                )}
              </div>
              <div className="p-4 border-t" style={{ borderColor: "#e5e0d8" }}>
                {teamLoading ? (
                  <div className="text-center py-2" style={{ color: "var(--steel)", fontSize: 13 }}>
                    🎯 Команда работает над задачей — {teamSelected.length} специалистов...
                  </div>
                ) : (
                  <button onClick={runTeam} disabled={!teamTask.trim() || teamSelected.length === 0} className="w-full py-3 rounded-lg text-sm font-medium" style={{ background: "var(--navy)", color: "var(--gold)", opacity: (!teamTask.trim() || teamSelected.length === 0) ? 0.4 : 1 }}>
                    🎯 Собрать мнения команды ({teamSelected.length}) →
                  </button>
                )}
              </div>
            </div>
          )}

          {/* ── STUDIO MODE (webdesign agent) ── */}
          {!teamMode && activeAgent?.id === "webdesign" && (
            previewHtml ? (
              /* Before / After */
              <div className="flex-1 flex flex-col md:flex-row gap-3 md:gap-4 overflow-hidden">
                {originalSiteUrl && (
                  <div className="flex-1 flex flex-col rounded-lg border overflow-hidden" style={{ borderColor: "#e5e0d8" }}>
                    <div className="px-4 py-2 border-b flex items-center gap-2" style={{ background: "var(--cream)", borderColor: "#e5e0d8" }}>
                      <span style={{ fontSize: 11, fontWeight: 700, color: "var(--steel)", letterSpacing: 1 }}>ДО</span>
                      <span style={{ fontSize: 10, color: "var(--steel)", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{originalSiteUrl}</span>
                    </div>
                    <div className="flex-1 overflow-hidden" style={{ background: "var(--cream-2)" }}>
                      {(() => {
                        const host = originalSiteUrl.replace(/^https?:\/\//, "").split("/")[0].toLowerCase();
                        const localBefore = host.includes("medeadent")
                          ? "/demo/gallery/medea-dent-moscow/before.png"
                          : host.includes("simbios")
                            ? "/demo/gallery/simbios-marketing-moscow/before.png"
                            : null;
                        return localBefore ? (
                          <img
                            src={localBefore}
                            alt="Оригинальный сайт"
                            style={{ width: "100%", height: "100%", objectFit: "cover", objectPosition: "top" }}
                          />
                        ) : (
                          <div
                            style={{
                              width: "100%",
                              height: "100%",
                              display: "flex",
                              alignItems: "center",
                              justifyContent: "center",
                              color: "var(--steel)",
                              fontFamily: "var(--font-mono)",
                              fontSize: 11,
                              letterSpacing: "0.14em",
                              textTransform: "uppercase",
                            }}
                          >
                            превью оригинала
                          </div>
                        );
                      })()}
                    </div>
                  </div>
                )}
                <div className="flex-1 flex flex-col rounded-lg border overflow-hidden" style={{ borderColor: "var(--gold)" }}>
                  <div className="px-4 py-2 border-b flex items-center justify-between" style={{ background: "var(--navy)", borderColor: "var(--gold)" }}>
                    <span style={{ color: "var(--gold)", fontSize: 11, fontWeight: 700, letterSpacing: 1 }}>✨ ПОСЛЕ — AI ЛЕНДИНГ</span>
                    <div className="flex items-center gap-2">
                      <button onClick={openFullscreen} style={{ color: "var(--gold)", fontSize: 11, background: "none", border: "1px solid var(--gold)", borderRadius: 4, padding: "3px 10px", cursor: "pointer" }}>⛶ На весь экран</button>
                      <button onClick={downloadHtml} style={{ color: "var(--gold)", fontSize: 11, background: "none", border: "1px solid var(--gold)", borderRadius: 4, padding: "3px 10px", cursor: "pointer" }}>↓ Скачать</button>
                      <button onClick={() => { setPreviewHtml(null); setOriginalSiteUrl(""); }} style={{ color: "var(--gold)", background: "none", border: "none", cursor: "pointer", fontSize: 18, lineHeight: 1 }}>✕</button>
                    </div>
                  </div>
                  <iframe srcDoc={previewHtml} style={{ flex: 1, border: 0, width: "100%", minHeight: 0 }} sandbox="allow-scripts" />
                </div>
              </div>
            ) : studioLoading ? (
              /* Editorial loader — center stage with rotating Ogilvy/Schwartz/Hopkins quotes */
              <div
                className="flex-1 flex flex-col rounded-lg overflow-hidden"
                style={{
                  background: "var(--navy)",
                  color: "var(--cream)",
                  position: "relative",
                }}
              >
                <div
                  className="flex-1 flex flex-col justify-center px-12 py-16"
                  style={{ position: "relative" }}
                >
                  <div
                    style={{
                      fontFamily: "var(--font-mono)",
                      fontSize: 11,
                      color: "var(--mint)",
                      letterSpacing: "0.28em",
                      textTransform: "uppercase",
                      marginBottom: 48,
                      transition: "opacity .6s ease",
                    }}
                    key={`role-${studioStep}`}
                  >
                    Сейчас работает · {STUDIO_QUOTES[Math.min(studioStep, STUDIO_QUOTES.length - 1)].role}
                  </div>
                  <blockquote
                    key={`quote-${studioStep}`}
                    style={{
                      fontFamily: "var(--font-serif)",
                      fontStyle: "italic",
                      fontSize: "clamp(28px, 3.2vw, 48px)",
                      lineHeight: 1.25,
                      color: "var(--cream)",
                      letterSpacing: "-0.02em",
                      fontWeight: 500,
                      margin: 0,
                      maxWidth: 820,
                      animation: "studioQuoteIn .7s ease",
                    }}
                  >
                    «{STUDIO_QUOTES[Math.min(studioStep, STUDIO_QUOTES.length - 1)].text}»
                  </blockquote>
                  <div
                    style={{
                      fontFamily: "var(--font-mono)",
                      fontSize: 12,
                      color: "rgba(245,243,239,0.55)",
                      letterSpacing: "0.14em",
                      marginTop: 24,
                      textTransform: "uppercase",
                    }}
                  >
                    — {STUDIO_QUOTES[Math.min(studioStep, STUDIO_QUOTES.length - 1)].author}
                  </div>
                </div>
                <div
                  className="px-12 py-8"
                  style={{
                    borderTop: "1px solid rgba(245,243,239,0.08)",
                    background: "var(--navy-2)",
                  }}
                >
                  <div
                    style={{
                      display: "flex",
                      justifyContent: "space-between",
                      fontFamily: "var(--font-mono)",
                      fontSize: 10,
                      letterSpacing: "0.14em",
                      textTransform: "uppercase",
                      marginBottom: 16,
                      color: "rgba(245,243,239,0.55)",
                    }}
                  >
                    {[
                      studioUrl ? "Читаю сайт" : "Анализирую",
                      "Методология",
                      "Дизайн",
                      "Оживляю",
                      "Готово",
                    ].map((s, i) => (
                      <span
                        key={i}
                        style={{
                          color: i <= studioStep ? "var(--gold)" : "rgba(245,243,239,0.35)",
                          fontWeight: i === studioStep ? 600 : 400,
                          transition: "color 0.5s ease",
                        }}
                      >
                        {`${String(i + 1).padStart(2, "0")} · ${s}`}
                      </span>
                    ))}
                  </div>
                  <div style={{ height: 2, background: "rgba(245,243,239,0.08)", borderRadius: 1 }}>
                    <div
                      style={{
                        height: "100%",
                        background: "var(--gold)",
                        borderRadius: 1,
                        width: `${((studioStep + 1) / 5) * 100}%`,
                        transition: "width 3.8s cubic-bezier(.4,0,.2,1)",
                      }}
                    />
                  </div>
                </div>
                <style>{`
                  @keyframes studioQuoteIn {
                    from { opacity: 0; transform: translateY(12px); }
                    to   { opacity: 1; transform: translateY(0); }
                  }
                `}</style>
              </div>
            ) : (
              /* Studio form */
              <div className="flex-1 flex flex-col rounded-lg border overflow-hidden" style={{ borderColor: "var(--line-2)", background: "#fff" }}>
                <div className="px-8 py-5 border-b" style={{ borderColor: "var(--line)" }}>
                  <div
                    style={{
                      fontFamily: "var(--font-mono)",
                      fontSize: 11,
                      color: "var(--mint)",
                      letterSpacing: "0.24em",
                      textTransform: "uppercase",
                      marginBottom: 10,
                    }}
                  >
                    Студия сайта
                  </div>
                  <div
                    style={{
                      fontFamily: "var(--font-serif)",
                      fontSize: 26,
                      fontWeight: 500,
                      color: "var(--navy)",
                      letterSpacing: "-0.015em",
                      lineHeight: 1.15,
                    }}
                  >
                    Бриф{" "}
                    <span style={{ fontStyle: "italic", color: "var(--steel)" }}>
                      для шестнадцати
                    </span>
                  </div>
                  <div style={{ fontSize: 13, color: "var(--steel)", marginTop: 6, fontStyle: "italic" }}>
                    Заполни → AI создаёт живой лендинг за 60 секунд
                  </div>
                </div>
                <div className="flex-1 overflow-y-auto p-6 flex flex-col gap-5">
                  <div>
                    <label style={{ fontFamily: "var(--font-mono)", fontSize: 11, color: "var(--steel)", display: "block", marginBottom: 8, letterSpacing: "0.14em", textTransform: "uppercase" }}>Название бизнеса</label>
                    <input className="w-full px-4 py-3 rounded text-sm outline-none" style={{ border: "1px solid var(--line-2)", background: "var(--cream)", color: "var(--navy)" }} placeholder="Simbios Marketing Agency" value={studioName} onChange={(e) => setStudioName(e.target.value)} />
                  </div>
                  <div>
                    <label style={{ fontFamily: "var(--font-mono)", fontSize: 11, color: "var(--steel)", display: "block", marginBottom: 8, letterSpacing: "0.14em", textTransform: "uppercase" }}>Что делают и для кого</label>
                    <textarea className="w-full px-4 py-3 rounded text-sm outline-none resize-none" style={{ border: "1px solid var(--line-2)", background: "var(--cream)", color: "var(--navy)" }} rows={3} placeholder="Маркетинговое агентство для малого бизнеса в Москве. Делаем SMM, контекст, сайты. Клиенты — рестораны, салоны, магазины." value={studioDesc} onChange={(e) => setStudioDesc(e.target.value)} />
                  </div>
                  <div>
                    <label style={{ fontFamily: "var(--font-mono)", fontSize: 11, color: "var(--steel)", display: "block", marginBottom: 8, letterSpacing: "0.14em", textTransform: "uppercase" }}>Отзывы с Яндекс.Карт <span style={{ color: "var(--mint)", fontWeight: 400, textTransform: "none", letterSpacing: 0 }}>— необязательно, но это WOW</span></label>
                    <textarea className="w-full px-4 py-3 rounded text-sm outline-none resize-none" style={{ border: "1px solid var(--line-2)", background: "var(--cream)", color: "var(--navy)" }} rows={4} placeholder={"Иван К.: Работаем 2 года, выручка выросла на 40%. Настоящие профессионалы!\nМария С.: Сделали SMM под ключ, клиентов стало вдвое больше за 3 месяца."} value={studioReviews} onChange={(e) => setStudioReviews(e.target.value)} />
                  </div>
                  <div>
                    <label style={{ fontFamily: "var(--font-mono)", fontSize: 11, color: "var(--steel)", display: "block", marginBottom: 8, letterSpacing: "0.14em", textTransform: "uppercase" }}>URL текущего сайта <span style={{ color: "var(--steel)", fontWeight: 400, textTransform: "none", letterSpacing: 0 }}>— для сравнения до/после</span></label>
                    <input className="w-full px-4 py-3 rounded text-sm outline-none" style={{ border: "1px solid var(--line-2)", background: "var(--cream)", color: "var(--navy)" }} placeholder="https://simbios-marketing.ru" value={studioUrl} onChange={(e) => setStudioUrl(e.target.value)} />
                  </div>
                </div>
                <div className="p-5 border-t" style={{ borderColor: "var(--line)" }}>
                  <button
                    onClick={runStudio}
                    disabled={!studioName && !studioDesc && !studioUrl}
                    className="w-full py-4 rounded text-sm font-medium"
                    style={{
                      background: "var(--navy)",
                      color: "var(--gold)",
                      opacity: (!studioName && !studioDesc && !studioUrl) ? 0.4 : 1,
                      fontFamily: "var(--font-sans)",
                      letterSpacing: "0.02em",
                    }}
                  >
                    {studioUrl ? "Переделать сайт за 60 секунд →" : "Создать лендинг за 60 секунд →"}
                  </button>
                </div>
              </div>
            )
          )}

          {/* ── REGULAR CHAT MODE (all other agents) ── */}
          {!teamMode && activeAgent?.id !== "webdesign" && (
            <div className={`flex-1 flex ${previewHtml ? "flex-row gap-4" : "flex-col"} overflow-hidden`}>
              {previewHtml && (
                <div className="flex-1 flex flex-col rounded-lg border overflow-hidden" style={{ borderColor: "var(--gold)" }}>
                  <div className="px-4 py-2 border-b flex items-center justify-between" style={{ borderColor: "var(--gold)", background: "var(--navy)" }}>
                    <span style={{ color: "var(--gold)", fontSize: 13, fontWeight: 600 }}>🌐 Предпросмотр</span>
                    <button onClick={() => setPreviewHtml(null)} style={{ color: "var(--gold)", fontSize: 16, background: "none", border: "none", cursor: "pointer" }}>✕</button>
                  </div>
                  <iframe srcDoc={previewHtml} className="flex-1 border-0 w-full" sandbox="allow-scripts" style={{ minHeight: 0 }} />
                </div>
              )}
              <div className={`${previewHtml ? "w-[420px] shrink-0" : "flex-1"} flex flex-col rounded-lg border overflow-hidden`} style={{ borderColor: "#e5e0d8", backgroundColor: "#fff" }}>
                {!activeAgent ? (
                  <div className="flex-1 flex flex-col justify-center px-12 py-16" style={{ position: "relative" }}>
                    <div
                      style={{
                        fontFamily: "var(--font-mono)",
                        fontSize: 10,
                        color: "var(--steel)",
                        letterSpacing: "0.24em",
                        textTransform: "uppercase",
                        marginBottom: 36,
                      }}
                    >
                      Ожидает брифа
                    </div>
                    <blockquote
                      style={{
                        fontFamily: "var(--font-serif)",
                        fontStyle: "italic",
                        fontSize: "clamp(32px, 3.4vw, 52px)",
                        lineHeight: 1.2,
                        color: "var(--navy)",
                        letterSpacing: "-0.02em",
                        fontWeight: 500,
                        margin: 0,
                        maxWidth: 720,
                      }}
                    >
                      «Первое впечатление — единственное.»
                    </blockquote>
                    <div
                      style={{
                        fontFamily: "var(--font-mono)",
                        fontSize: 11,
                        color: "var(--steel)",
                        letterSpacing: "0.14em",
                        marginTop: 20,
                      }}
                    >
                      — David Ogilvy, 1963
                    </div>
                    <div
                      style={{
                        marginTop: 56,
                        paddingTop: 32,
                        borderTop: "1px solid var(--line)",
                        maxWidth: 720,
                      }}
                    >
                      <div
                        style={{
                          fontSize: 11,
                          color: "var(--mint)",
                          letterSpacing: "0.24em",
                          textTransform: "uppercase",
                          fontWeight: 500,
                          marginBottom: 20,
                        }}
                      >
                        16 агентов · выбери слева
                      </div>
                      <div
                        style={{
                          display: "grid",
                          gridTemplateColumns: "repeat(8, 1fr)",
                          gap: 10,
                          maxWidth: 360,
                        }}
                      >
                        {Array.from({ length: 16 }).map((_, i) => (
                          <div
                            key={i}
                            style={{
                              width: 8,
                              height: 8,
                              borderRadius: "50%",
                              background: "var(--gold)",
                              opacity: 0.25 + (i % 4) * 0.15,
                              animation: `dotPulse 2.8s ease-in-out ${i * 0.18}s infinite`,
                            }}
                          />
                        ))}
                      </div>
                    </div>
                    <style>{`
                      @keyframes dotPulse {
                        0%, 100% { opacity: 0.2; }
                        50% { opacity: 0.85; }
                      }
                    `}</style>
                  </div>
                ) : (
                  <>
                    <div className="px-6 py-4 border-b flex items-center gap-3" style={{ borderColor: "#e5e0d8" }}>
                      <span style={{ color: "var(--navy)", display: "inline-flex" }}>{AGENT_ICONS[activeAgent.id]}</span>
                      <div>
                        <div style={{ fontWeight: 600, color: "var(--navy)", fontSize: 15 }}>{activeAgent.title}</div>
                        <div style={{ fontSize: 11, color: "var(--steel)" }}>{activeAgent.desc}</div>
                      </div>
                    </div>
                    <div className="flex-1 overflow-y-auto p-6 flex flex-col gap-4">
                      {messages.map((msg, i) => {
                        const isUser = msg.role === "user";
                        const splitMatch = !isUser ? msg.content.split(/\n---\n\s*\*\*🧠/i) : null;
                        const mainText = splitMatch ? splitMatch[0].trim() : msg.content;
                        const methodology = splitMatch && splitMatch[1] ? splitMatch[1].replace(/^\s*Почему я так сделал:\*\*\s*/i, "").trim() : null;
                        const isLastAssistant = !isUser && i === messages.length - 1 && i > 0;
                        const handoffTargets = isLastAssistant && activeAgent ? (HANDOFFS[activeAgent.id] || []).map(id => AGENTS.find(a => a.id === id)).filter(Boolean) : [];
                        return (
                          <div key={i} className={`flex flex-col gap-2 ${isUser ? "items-end" : "items-start"}`}>
                            <div className="max-w-lg px-4 py-3 rounded-lg text-sm leading-relaxed whitespace-pre-wrap" style={{ backgroundColor: isUser ? "var(--navy)" : "var(--cream)", color: isUser ? "var(--cream)" : "var(--navy)", borderRadius: isUser ? "16px 16px 4px 16px" : "16px 16px 16px 4px" }}>
                              {mainText}
                            </div>
                            {methodology && (
                              <div className="max-w-lg px-4 py-3 rounded-lg text-xs leading-relaxed whitespace-pre-wrap" style={{ backgroundColor: "rgba(200,183,156,0.12)", color: "var(--navy)", border: "1px solid var(--gold)", borderRadius: "12px 12px 12px 4px" }}>
                                <div style={{ fontWeight: 700, color: "var(--gold)", marginBottom: 6, letterSpacing: 0.5, fontSize: 10, textTransform: "uppercase" }}>🧠 Почему я так сделал</div>
                                {methodology}
                              </div>
                            )}
                            {isLastAssistant && handoffTargets.length > 0 && (
                              <div className="flex flex-wrap gap-2 mt-1 max-w-lg">
                                <span style={{ fontSize: 10, color: "var(--steel)", letterSpacing: 1, alignSelf: "center" }}>ПЕРЕДАТЬ →</span>
                                {handoffTargets.map(agent => agent && (
                                  <button key={agent.id} onClick={() => handoffToAgent(agent.id)} className="text-xs px-3 py-1.5 rounded-full border transition-all hover:scale-105" style={{ backgroundColor: "#fff", borderColor: "var(--gold)", color: "var(--navy)" }}>
                                    <span className="inline-flex items-center gap-1.5">{AGENT_ICONS[agent.id]} {agent.title}</span>
                                  </button>
                                ))}
                              </div>
                            )}
                          </div>
                        );
                      })}
                      {loading && (
                        <div className="flex justify-start">
                          <div className="px-4 py-3 rounded-lg text-sm" style={{ background: "var(--cream)", color: "var(--steel)" }}>Думаю...</div>
                        </div>
                      )}
                    </div>
                    <div className="p-4 border-t flex gap-3" style={{ borderColor: "#e5e0d8" }}>
                      <input className="flex-1 px-4 py-3 rounded-lg border text-sm outline-none" style={{ borderColor: "#e5e0d8", background: "var(--cream)", color: "var(--navy)" }} placeholder={`Спросите ${activeAgent.title}...`} value={input} onChange={(e) => setInput(e.target.value)} onKeyDown={(e) => e.key === "Enter" && sendMessage()} />
                      <button onClick={sendMessage} disabled={loading} className="px-5 py-3 rounded-lg text-sm font-medium" style={{ background: "var(--navy)", color: "var(--gold)" }}>→</button>
                    </div>
                  </>
                )}
              </div>
            </div>
          )}

        </div>
      </main>

      <footer
        style={{
          background: "var(--navy)",
          color: "rgba(245,243,239,0.55)",
          padding: "18px 0",
          fontSize: 11,
          borderTop: "1px solid rgba(245,243,239,0.08)",
        }}
      >
        <div
          style={{
            maxWidth: 1240,
            margin: "0 auto",
            padding: "0 32px",
            display: "flex",
            justifyContent: "space-between",
            flexWrap: "wrap",
            gap: 12,
          }}
        >
          <div>© 2026 natashabrovkina.com · ИП Бровкина&nbsp;Н.</div>
          <div style={{ display: "flex", gap: 18, flexWrap: "wrap" }}>
            <Link href="/method" style={{ color: "inherit", textDecoration: "none" }}>Метод</Link>
            <Link href="/gallery" style={{ color: "inherit", textDecoration: "none" }}>Кейсы</Link>
            <Link href="/pricing" style={{ color: "inherit", textDecoration: "none" }}>Тарифы</Link>
            <a
              href="https://t.me/NATASHABROVKINA"
              target="_blank"
              rel="noopener noreferrer"
              style={{ color: "inherit", textDecoration: "none" }}
            >
              Telegram @NATASHABROVKINA
            </a>
          </div>
        </div>
      </footer>

      {/* Brand Memory modal */}
      {showBrand && (
        <div className="fixed inset-0 flex items-center justify-center p-4 z-50" style={{ backgroundColor: "rgba(13,27,42,0.7)" }} onClick={() => setShowBrand(false)}>
          <div className="w-full max-w-2xl rounded-xl border max-h-[90vh] overflow-y-auto" style={{ backgroundColor: "#fff", borderColor: "#e5e0d8" }} onClick={(e) => e.stopPropagation()}>
            <div className="sticky top-0 px-8 pt-8 pb-4" style={{ backgroundColor: "#fff", borderBottom: "1px solid #e5e0d8" }}>
              <div className="flex items-start justify-between mb-2">
                <div>
                  <h2 style={{ fontFamily: "var(--font-serif)", fontSize: 22, fontWeight: 700, color: "var(--navy)" }}>Мой бизнес</h2>
                  <p style={{ color: "var(--steel)", fontSize: 13, marginTop: 4 }}>Заполни один раз — все 16 агентов будут это знать и использовать</p>
                </div>
                <button onClick={() => setShowBrand(false)} style={{ color: "var(--steel)", fontSize: 24, background: "none", border: "none", cursor: "pointer", lineHeight: 1 }}>✕</button>
              </div>
            </div>
            <div className="px-8 py-6 flex flex-col gap-5">
              <div>
                <label style={{ fontSize: 11, fontWeight: 700, color: "var(--navy)", display: "block", marginBottom: 10, letterSpacing: 1 }}>ТИП БИЗНЕСА <span style={{ color: "var(--gold)", fontWeight: 400 }}>(применяет вертикальную экспертизу)</span></label>
                <div className="grid grid-cols-3 gap-2">
                  {BUSINESS_TYPES.map(t => (
                    <button
                      key={t.id}
                      onClick={() => setBrand({ ...brand, type: t.id })}
                      className="text-left px-3 py-2 rounded-lg border transition-all"
                      style={{
                        backgroundColor: brand.type === t.id ? "var(--navy)" : "var(--cream)",
                        borderColor: brand.type === t.id ? "var(--gold)" : "#e5e0d8",
                        color: brand.type === t.id ? "var(--gold)" : "var(--navy)",
                      }}
                    >
                      <div style={{ fontSize: 16, marginBottom: 2 }}>{t.icon}</div>
                      <div style={{ fontSize: 11, fontWeight: 600 }}>{t.label}</div>
                    </button>
                  ))}
                </div>
              </div>
              {[
                { key: "name", label: "НАЗВАНИЕ БИЗНЕСА", placeholder: "Simbios Marketing Agency", rows: 1 },
                { key: "what", label: "ЧТО ПРОДАЁМ", placeholder: "SMM, контекст, разработка сайтов", rows: 2 },
                { key: "who", label: "КОМУ ПРОДАЁМ (ЦА)", placeholder: "Рестораны, салоны красоты, клиники в Москве. Чек от 80к.", rows: 2 },
                { key: "usp", label: "ЧЕМ ОТЛИЧАЕМСЯ", placeholder: "Погружаемся в нишу, еженедельные отчёты, работаем с 2019 года", rows: 2 },
                { key: "voice", label: "ТОН ГОЛОСА", placeholder: "Профессионально, без канцелярита. На \"ты\" с предпринимателями.", rows: 2 },
                { key: "cases", label: "КЕЙСЫ С ЦИФРАМИ", placeholder: "3× поток клиентов у салона за 2 года. +40% прибыли ресторана за месяц.", rows: 3 },
                { key: "competitors", label: "КОНКУРЕНТЫ", placeholder: "Simbios, DigitalHunter, RedFox (ссылки или описание)", rows: 2 },
              ].map(f => (
                <div key={f.key}>
                  <label style={{ fontSize: 11, fontWeight: 700, color: "var(--navy)", display: "block", marginBottom: 6, letterSpacing: 1 }}>{f.label}</label>
                  {f.rows === 1 ? (
                    <input className="w-full px-4 py-3 rounded-lg border text-sm outline-none" style={{ borderColor: "#e5e0d8", background: "var(--cream)", color: "var(--navy)" }} placeholder={f.placeholder} value={brand[f.key as keyof typeof brand]} onChange={(e) => setBrand({ ...brand, [f.key]: e.target.value })} />
                  ) : (
                    <textarea className="w-full px-4 py-3 rounded-lg border text-sm outline-none resize-none" style={{ borderColor: "#e5e0d8", background: "var(--cream)", color: "var(--navy)" }} rows={f.rows} placeholder={f.placeholder} value={brand[f.key as keyof typeof brand]} onChange={(e) => setBrand({ ...brand, [f.key]: e.target.value })} />
                  )}
                </div>
              ))}
            </div>
            <div className="sticky bottom-0 px-8 py-5 flex gap-3" style={{ backgroundColor: "#fff", borderTop: "1px solid #e5e0d8" }}>
              <button onClick={saveBrand} className="flex-1 py-3 rounded-lg text-sm font-medium" style={{ background: "var(--navy)", color: "var(--gold)" }}>
                💾 Сохранить — все агенты применят
              </button>
              <button onClick={() => { setBrand({ type: "", name: "", what: "", who: "", usp: "", voice: "", cases: "", competitors: "" }); localStorage.removeItem("brand_memory"); }} className="px-5 py-3 rounded-lg text-sm" style={{ color: "var(--steel)", background: "none", border: "1px solid #e5e0d8" }}>
                Очистить
              </button>
            </div>
          </div>
        </div>
      )}

      {/* BYOK modal */}
      {showKeyModal && (
        <div
          className="fixed inset-0 flex items-center justify-center p-4 z-50"
          style={{ backgroundColor: "rgba(13,27,42,0.7)" }}
          onClick={() => setShowKeyModal(false)}
        >
          <div
            className="w-full max-w-md rounded-xl border p-8"
            style={{ backgroundColor: "#fff", borderColor: "#e5e0d8" }}
            onClick={(e) => e.stopPropagation()}
          >
            <h2 style={{ fontFamily: "var(--font-serif)", fontSize: 20, fontWeight: 700, color: "var(--navy)", marginBottom: 8 }}>
              {apiKey ? "Ваш API-ключ" : "Подключить свой ключ"}
            </h2>
            <p style={{ color: "var(--steel)", fontSize: 13, marginBottom: 20, lineHeight: 1.6 }}>
              {apiKey
                ? "Вы используете свой ключ Anthropic. Ключ хранится только в браузере."
                : <>Без ключа работает бесплатно (лимит). Свой ключ — без ограничений.<br />
                    Получить: <a href="https://console.anthropic.com" target="_blank" rel="noopener noreferrer" style={{ color: "var(--gold)" }}>console.anthropic.com</a></>
              }
            </p>
            {!apiKey && (
              <>
                <input
                  type="password"
                  className="w-full px-4 py-3 rounded-lg border text-sm outline-none mb-3"
                  style={{ borderColor: "#e5e0d8", background: "var(--cream)", color: "var(--navy)" }}
                  placeholder="sk-ant-api03-..."
                  value={keyInput}
                  onChange={(e) => setKeyInput(e.target.value)}
                  onKeyDown={(e) => e.key === "Enter" && (saveKey(), setShowKeyModal(false))}
                />
                {keyError && <p style={{ color: "#c0392b", fontSize: 12, marginBottom: 12 }}>{keyError}</p>}
                <button
                  onClick={() => { saveKey(); if (!keyError) setShowKeyModal(false); }}
                  className="w-full py-3 rounded-lg text-sm font-medium mb-3"
                  style={{ background: "var(--navy)", color: "var(--gold)" }}
                >
                  Сохранить ключ →
                </button>
              </>
            )}
            {apiKey && (
              <button
                onClick={() => { resetKey(); setShowKeyModal(false); }}
                className="w-full py-3 rounded-lg text-sm font-medium mb-3"
                style={{ background: "var(--cream)", color: "var(--navy)", border: "1px solid #e5e0d8" }}
              >
                Удалить ключ (переключиться на бесплатный)
              </button>
            )}
            <button
              onClick={() => setShowKeyModal(false)}
              className="w-full py-2 text-sm"
              style={{ color: "var(--steel)", background: "none", border: "none", cursor: "pointer" }}
            >
              Закрыть
            </button>
          </div>
        </div>
      )}
    </div>
  );
}
